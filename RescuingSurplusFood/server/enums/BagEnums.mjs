// Enum for bag type
const BagType = Object.freeze({
  SURPRISE: "surprise",
  REGULAR: "regular",
});

// Enum for bag size
const BagSize = Object.freeze({
  SMALL: "small",
  MEDIUM: "medium",
  LARGE: "large",
});

// Enum for bag status
const BagStatus = Object.freeze({
  AVAILABLE: "available",
  RESERVED: "reserved",
});

export { BagType, BagSize, BagStatus };
